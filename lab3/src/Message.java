/**
 * interface that is used for messages<br>
 * it has one method to send the message
 */
public interface Message {
    /**
     * Method send() is used to send message
     */
    String send ();
}
